export const metadata = {
  title: "Amelise — Development Proposal",
  description: "V1 MVP Development Quote",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
